import PostLayout from '../../src/components/layout/PostLayout';
import { useCallback } from 'react';
import { Heading } from '@chakra-ui/react';

export const metadata = {
  title: 'Context API con typescript',
  slug: 'context-api-con-typescript',
  date: '2022-01-14T19:02:51.838Z',
};

<Heading
  as="h2"
  bgGradient="linear(to-r, #ffff80, #ff80bf)"
  _light={{
    bgGradient: 'linear(to-r, #ff80bf, #9580ff)',
  }}
  bgClip="text"
  mb={4}
>
  Context API con typescript
</Heading>

Vamos a crear una variable con todos los valores mencionado anteriormente.

```js
const year = '2001';
const movie = 'Shrek';

console.log(`${movie}, ${year}`);
```

```typescript
Context.Provider({ year, movie });
```

export default ({ children, ...props }) => <PostLayout>{children}</PostLayout>;
